set-secret:
  description: Test action to set a secret key/value pair.
  key:
    type: string
    description: The key for the secret
  value:
    type: string
    description: The value for the secret

set-secret-labelled:
  description: Test action to set a secret key/value pair with a label on the secret.
  key:
    type: string
    description: The key for the secret
  value:
    type: string
    description: The value for the secret
